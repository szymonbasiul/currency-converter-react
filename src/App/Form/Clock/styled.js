import styled from "styled-components";

export const ClockForm = styled.p`
  font-size: 14px;
  margin: 2% 5%;
  display: flex;
  font-family: monospace;
  align-self: self-end;
  color: ${({ theme }) => theme.color.darkGrey};
`;
